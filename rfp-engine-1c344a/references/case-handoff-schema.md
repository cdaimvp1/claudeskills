# Case Handoff Schema - rfp-engine → rfp-case-manager

> **SOURCE OF TRUTH. Do not hand-edit any copy of this schema.**
>
> This file is the single source of truth for `case_handoff.json`. rfp-engine PRODUCES the
> payload and rfp-case-manager CONSUMES it, so the producer owns the schema.
>
> `rfp-case-manager-1c344a/SKILL.md` carries an inlined MIRROR of the JSON block below,
> because that skill must be readable standalone. That mirror is a copy, not a second
> authority. To change the schema: edit this file, then re-copy the JSON block into the
> mirror in the same commit, and say in the commit message that both moved.
>
> Divergence found and reconciled 2026-07-29 (E1). It had drifted in BOTH directions, which
> is why neither copy could be treated as simply correct: this file was missing the
> `delivery_method` graceful-degradation wording, and the mirror was missing the `Addendum`
> and `QA_Log` artifact types. The mirror also carried a note asserting that this file
> "still describes legacy provisioning actions on receipt", which had itself gone stale,
> since this file already states the no-provisioning behavior. All three are fixed.

Schema for `case_handoff.json`. This payload is generated by rfp-engine when user selects case handoff. rfp-case-manager consumes it to initialize or update the case file. If a Microsoft Team is bound, it maps artifacts to the existing Teams/SharePoint structure. It does not create SharePoint folders, provision Teams channels, move files, send notifications, or return a live case URL.

---

## Schema

```json
{
  "schema_version": "1.0",
  "handoff_timestamp": "ISO-8601 datetime",
  "source_skill": "rfp-engine",

  "case": {
    "case_id": "string - generated by rfp-engine as RFP-{YYYYMMDD}-{category-slug}",
    "title": "string - human-readable RFP name",
    "category": "string - sourcing category (e.g., Enterprise SaaS, Professional Services)",
    "description": "string - 2-3 sentence summary of what is being sourced",
    "status": "Draft | Issued | In Evaluation | Awarded | Cancelled",
    "mode": "Brief | Full",
    "created_by": "string - Lilly user who initiated the RFP"
  },

  "artifacts": {
    "base_path": "string - relative or SharePoint path where artifacts are stored",
    "files": [
      {
        "artifact_id": "string (e.g., A-01)",
        "filename": "string (e.g., RFP_Instructions.docx)",
        "type": "RFP_Instructions | Requirements_Matrix | Schedule | Pricing_Template | Demo_Evaluation_Guide | Post_Award_Timeline | Stakeholder_Roster | Addendum | QA_Log",
        "path": "string - full path to file",
        "status": "Draft | Final | Issued"
      }
    ],
    "invitation_email": {
      "subject": "string - email subject line",
      "body": "string - email body text with [Vendor Name] / [Vendor Contact] placeholders for per-vendor substitution",
      "delivery_method": "actionable draft in chat (no auto-send; if message_compose is unavailable, falls back to a labeled inline draft)",
      "vendor_specific_variants": "boolean - true if per-vendor emails were produced with contact info pre-filled"
    }
  },

  "schedule": {
    "rfp_release_date": "ISO-8601 date",
    "qa_deadline": "ISO-8601 date",
    "qa_response_date": "ISO-8601 date",
    "proposals_due": "ISO-8601 date",
    "evaluation_start": "ISO-8601 date",
    "evaluation_end": "ISO-8601 date",
    "demo_window_start": "ISO-8601 date",
    "demo_window_end": "ISO-8601 date",
    "award_target": "ISO-8601 date",
    "contract_execution_target": "ISO-8601 date"
  },

  "suppliers": [
    {
      "supplier_id": "string (e.g., SUP-01)",
      "name": "string",
      "status": "Invited | Confirmed | Declined | No Response",
      "primary_contact": {
        "name": "string",
        "email": "string",
        "title": "string"
      },
      "source": "Supplier Landscape | User-Specified | Incumbent",
      "landscape_rank": "integer | null - ranking from supplier-landscape if applicable"
    }
  ],

  "stakeholders": [
    {
      "name": "string",
      "title": "string",
      "function": "string",
      "evaluation_focus": ["string"],
      "demo_attendance": true,
      "approval_authority": false
    }
  ],

  "requirements_summary": {
    "total_requirements": "integer",
    "must_have_count": "integer",
    "should_have_count": "integer",
    "nice_to_have_count": "integer",
    "categories": ["string - list of requirement categories"]
  },

  "flags": {
    "incumbent_present": "boolean",
    "incumbent_name": "string | null",
    "legal_terms_in_scope": "boolean",
    "regulatory_requirements": ["string"],
    "draft_requirements": "boolean - true if requirements matrix contains Draft_Flag = TRUE rows"
  },

  "downstream_routing": {
    "next_skill": "rfp-case-manager",
    "auto_advance_on_completion": true,
    "pipeline_position": 2
  }
}
```

---

## Validation Rules

rfp-case-manager must validate on receipt:

| Check | Rule |
|-------|------|
| `schema_version` | Must be "1.0" |
| `case.case_id` | Must be unique - reject duplicates |
| `artifacts.files` | All paths must be resolvable |
| `schedule.proposals_due` | Must be after `schedule.qa_deadline` |
| `suppliers` | At least 1 supplier with status "Invited" |
| `stakeholders` | At least 1 with `approval_authority = true` |

---

## rfp-case-manager Actions on Receipt

1. Validate the handoff payload.
2. Create or update the local case file.
3. Map artifact paths to the case file.
4. If a Team is bound, summarize the existing SharePoint/Teams structure and recommend where artifacts belong.
5. Generate scheduling/status draft text for the user to execute manually.
6. Confirm case initialization to the user.

---